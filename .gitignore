node_modules/
*.log
.DS_Store
Thumbs.db
.env
.env.*
*.local
dist/
*.zip
.vscode/
.idea/

# bot runtime state (replied ids, since_id)
bot/state/

# Internal working notes, never published
HANDOFF.md

# pt-recon raw captures + dossiers: contain cookies, auth headers, balances,
# wallet ids. NEVER commit. Sanitized fixtures are copied out deliberately.
# (*.local above already covers recon-data/DENYLIST.local.)
recon-data/
tools/recon/.headless/node_modules/

# wrangler local state — never commit (may embed oauth/session material)
server/.wrangler/

# Local screenshot captures and headless Chrome profiles. Never publish.
site/_shots/

# Local showcase film factory (captures, licensed-or-not audio, renders)
showcase-film/

